import React from 'react';

import {
  SelectWrapper
} from './select.styles';

export interface selectProps {

}

export const Select = ({}: selectProps) => {
  return (
    <SelectWrapper>
      
    </SelectWrapper>
  );
};
